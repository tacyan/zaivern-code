# CLAUDE.md — Zaivern Code 開発ガイド（AIエージェント向け）

このファイルはリポジトリで作業する全ての AI エージェント／開発者が最初に読むこと。

## 絶対ルール

- **ハードコーディング禁止。** どの環境（macOS / Windows / Linux、任意のユーザー名・ホーム・ロケール）でも動くコードのみ。
  - パスは `std::env::temp_dir()` / `dirs` クレート / 設定から導出。`/tmp`・`/Users/...`・`C:\` の直書き禁止。
  - OS 差分は `cfg!(windows)` 等で分岐し、**両側を実装**する。
  - エージェント固有値（resume フラグ、コマンド名等）は `agents.rs` のカタログにデータとして持つ。
- **egui 0.29 固定**（アップグレード禁止）・rustc 1.88+。
- **vendor/vt100 はパッチ済みベンダリング。** `visible_rows` 修正を外すと deep scrollback で debug パニックが再発する。バージョンアップ時は必ずパッチを移植。
- main へ直接コミットしない。作業はブランチ＋隔離ワークツリー（`.claude/worktrees/`）で行う（複数の AI エージェントが同時編集する前提）。
- **統合したワークツリーは即座に消す。** ブランチが統合先の祖先になった時点で
  `git worktree remove` + `git branch -D` まで行う。各ワークツリーは独立した
  `target/` を持つため、放置すると**1つあたり数 GB**積み上がる
  (実測: 15 個で 47GB)。ディスクだけの問題ではなく、ページキャッシュが
  押し出されてテスト実行が数倍遅くなる。
- **`git stash` を使わない。** stash スタックは全ワークツリーで共有されるため、
  同時に動いている別のインスタンスの退避を巻き込む。退避が要るなら WIP コミットを切る。
- **UI から到達できない実装は「未完成」。** 完了報告の前に `cargo check` の
  `never used` 警告を必ず確認し、自分が追加した公開関数・型・コマンドが
  実際に呼ばれていることを確かめる。この警告は「作ったのに繋いでいない」の検出器である。
  到達経路（パレット / メニュー / ボタン / キーバインドのいずれか）を必ず1つ以上用意し、
  用意できないなら README には機能として書かず、未完項目として残す。
- **機能を増やす前に減らせないかを考える。** 同じ操作への到達経路が3つあるなら
  2つ削る。常に0を表示するバッジ、中身より空状態を見せる時間が長いパネル、
  ツールバーとヘッダで重複するボタンは、実装した本人が消す。

## テストと検証

- 検証は**ローカル優先**。CI は最小構成 + `Swatinem/rust-cache` 必須。
- **Linux 側は毎回 `tools/linux-test.sh` で確認する。** macOS だけで開発していると
  **Linux / Windows でしか出ない不具合が素通りする**。実際に 43,000 行の変更が
  macOS では全部緑なのに CI の Linux / Windows で落ちた
  (`keybinds::canonical_mods` が非 macOS で ⌃ を ⌘ へ畳むため、
   `⌃⌘F` と `⌘F` が同じ打鍵になり片方が永久に効かなくなっていた)。
  CI 往復は 1 周 5〜6 分かかるうえ切り分けに要る情報が出てこないが、
  Docker なら **1 周 30 秒**で回せる。
  - `tools/linux-test.sh` / `tools/linux-test.sh keybinds::` / `tools/linux-test.sh --check`
  - **ホストの `target/` を汚さないこと** (同じディレクトリを使うと Linux の成果物で
    macOS のビルドが無効化され、戻るたびにフルビルドが走る)。スクリプトは
    `CARGO_TARGET_DIR` を分けている
  - **Docker でだけ落ちるものは追わない**: `app::glyph_tests::*` (フォント無し) /
    `cli::tests::instance_current_uses_own_pid` (PID 名前空間) /
    `terminal::reap_pty_tests::*` (コンテナ内 PTY)。CI の ubuntu では通る
- **Windows 側は `tools/windows-check.sh` で確認する。** `#[cfg(windows)]` のコードは
  macOS のビルドで**一度もコンパイルされない** (src/ の 23 ファイルに 87 箇所ある)。
  手元の `cargo check` が全部緑でも、その中身は未検証のまま残る。
  - `tools/windows-check.sh` — `cargo xwin check --all-targets` (既定・最速)
  - `tools/windows-check.sh --build` — 実際に PE32+ の `zai.exe` を作る (リンクまで通る)
  - `tools/windows-check.sh --clippy` — CI と同じ債務リストで clippy。
    **CI の lint は ubuntu 1 台でしか回らないので、Windows 限定の警告はここでしか出ない**
    (実際に `file_tree.rs` の unused import 2 件がこれで見つかった)
  - `tools/windows-check.sh --gnu` — mingw-w64 経由の `x86_64-pc-windows-gnu`。
    xwin を入れたくない環境向け。src/ に `target_env` の分岐は 1 つも無いので
    cfg 的には msvc と等価
  - 初回だけ `cargo install cargo-xwin --locked` が要る (無ければ手順を出して終了する)。
    素の `cargo check --target x86_64-pc-windows-msvc` は syntect が引く C の
    `onig_sys` が `stdlib.h` を見つけられずに落ちるため**使えない**
  - **担保できないもの**: 実行時の挙動と GUI、それに build.rs の VERSIONINFO 埋め込み。
    Cargo は `[target.'cfg(windows)'.build-dependencies]` を**ホスト**で評価するので、
    macOS からのクロスビルドでは winresource が入らない。ここは CI の windows-latest だけが番人
- **OS で分岐する既定値は、テストも OS 条件を明示する。** `#[cfg(target_os = "macos")]` か
  `cfg!(...)` を期待値に入れる。macOS の予約表と突き合わせるテストを非 mac で走らせると、
  「その OS では使わない打鍵」を咎めるだけになる。
- `terminal::` は実 PTY テスト。1 つの `cargo test` プロセス内で走らせると子プロセスツリーが蓄積し **Linux ランナーを殺す**ため、CI は cargo-nextest（テスト毎プロセス + `pty` test-group で直列化 + slow-timeout のプロセスグループ kill）で実行する。設定は `.config/nextest.toml`。
- ローカル実行例: `cargo nextest run --profile ci`（全量）/ `cargo test session::`（モジュール別）。
- テストは `crate::test_util::unique_temp_dir` を使い、実 `~/.zaivern` に触れない。
- GUI の動作検証はプロセス生存確認で行う（ヘッドレスでは UI を目視できないため）。
- **終了済みセッションへ kill を撃たない**（PID 再利用の巻き添え防止。既存ガードを regress させない）。

## UI の原則

- **画面が突然変わらない。** 何かを起動しただけでレイアウトが激変してはいけない。
  新しい対象はハイライトとスクロール追従で示し、大きな領域を開くのは明示的な操作（Enter 等）を待つ。
- **空白は作らない。** 中身が空のセクションは高さを確保せず、見出しごと消すか畳む。
  空状態は利用可能領域の**中央**に1枚のカードで示す（下や上に取り残さない）。
- **どの幅でも見切れない。** 行は `ui.available_width()` に必ず収める。
  ボタン列は折り返すか、狭いときはアイコンのみへ縮退させる。長い文字列は省略してホバーで全文。
- **中央ビューは単一の列挙型で持つ。** 独立した bool を複数持つと、2つのビューが同時に
  描かれる事故が構造的に起こり得る（実際に起きた）。
- **レイアウト判断は純粋関数に切り出してテーブルテストで固定する。**
  可用幅・件数・最長ラベル幅 → 列幅／折り返し／空状態の有無、そして
  **全ての矩形が可用領域内に収まり重ならないこと**を、極端なサイズ（900×700、1200×300 等）で検証する。

## 既知の罠

- egui-winit 0.29 はペーストコード（⌘V / Ctrl+V）の **press イベントを飲み込む** — クリップボード処理は release イベント側で検知する。
- **飲み込まれるのは V だけではない。** `egui-winit-0.29.1/src/lib.rs:758-774` は
  押下イベントが `modifiers.command && key ∈ {X, C, V}` に当たると
  `Event::Cut / Copy / Paste` を積んで **その場で return** する。判定は
  **shift / alt の有無を見ていない**ので、`⌘⇧C`（Cockpit）や `⌘⇧V`（プレビュー）は
  `consume_shortcut` では**構造的に絶対発火しない**（実際に「ツールチップに ⌘⇧C と
  出ているのに効かない」として報告された）。`command` は mac では ⌘、
  Windows/Linux では **Ctrl** に写るので、`Ctrl+Shift+C` も同じく死ぬ。
  ショートカットの消費は必ず `keybinds::consume_shortcut_compat` を通すこと
  （すり替えを逆再生して拾い直す。素の ⌘C/⌘V は横取りしない）。
- **画面に出す打鍵表記をベタ書きしない。** `tr("… (⌘⇧C)")` は
  (1) config.toml で再割り当てされた瞬間に嘘になり (2) Windows/Linux では表記そのものが違う。
  必ず `keybinds::format_shortcut(keys.get(BindAction::X))`（app 内は `self.key_hint`,
  `Keybinds` を持てない描画側は `keybinds::key_hint(ctx, ..)`）から生成する。
  `keybinds::tests::画面のショートカット表記をベタ書きしていない` が番人。
- **キーバインドは macOS の予約表と突き合わせる。** `keybinds::MACOS_RESERVED` は
  `~/Library/Preferences/com.apple.symbolichotkeys.plist` の enabled 項目から起こした実測表。
  `⌘⌥D`（Dock）だけでなく **`⌃Space`（前の入力ソース: 日本語 IME を使うと必ず有効）**、
  `⌘\``（次のウィンドウ）、`⌘Space` / `⌥⌘Space` なども取られている。
- **ソースを読む回帰テストは改行を正規化する。** `include_str!` の結果を
  `.replace("\r\n", "\n")` に通してから検索する。Windows のチェックアウトは CRLF なので、
  `"\n        });\n"` のようなパターンは必ず外れる。
- **macOS の `⌘⌥D` は OS（Dock の自動的に隠す）に予約されている。** アプリまで届かない。
  キーバイントを決めるときは OS の予約と既存割り当ての両方を確認する。
- **端末セルは整数ピクセルに揃える。** `col * cell_w` を小数のまま使うと、epaint が
  文字位置だけを丸めるため桁間隔が 8/8/7/8 px と揺れ、文字がガタガタに見える。
  100% 表示（Windows に多い）で最も悪化する。`theme::snap_len` を通すこと。
- **プロセスを殺すときは必ずツリーごと。** 直接の子だけを kill すると、シェルが
  `exec` せずに起動した孫がパイプを握ったまま残り、読み取りの join が孫の寿命まで戻らない
  （= UI が固まる）。`procx::kill_tree` を使い、unix では子を独立したプロセスグループで起動する。
- **`kill` に負の PID を渡すときは `--` を付ける。** Linux の procps-ng は `-<pid>` を
  短オプションの塊として解釈し、PID が 1 で始まると `kill(-1, SIGKILL)` になって
  ユーザーの全プロセスを巻き込む（実際に起きた）。
- **egui 0.29 の ID はラベル文字列由来ではない。** `Button` / `SelectableLabel` /
  `Checkbox` / `RadioButton` / `Link` / salt 無しの `TextEdit` は `allocate_*` 経由の
  **自動カウンタ**から ID を作るので、同じラベルが並んでも衝突しない
  （同一ラベル5個 → ID は5種類。ヘッドレステストで実証済み）。
  本当に衝突するのは `make_persistent_id` を通る系統だけ:
  `ScrollArea` / `ComboBox` / `CollapsingHeader` / `Grid` / salt 付き `TextEdit`。
  可変長リストの中でこれらを使うときは `ui.push_id` か salt に要素の ID を混ぜる
  （`src/panels.rs` の `mod egui_id_guard` が構造検査で自動検出する）。
  **「同じ見た目のボタンが並ぶと最後しか押せない」は、この egui では起こらない。**
  そう見えたら原因はアプリ側のロジック（index と ID の取り違え等）なので、そちらを疑うこと。
- **画面テキストの部分一致で状態を判定しない。** `error` / `failed` を部分一致で拾うと
  `Read(src/error_handling.rs)` や `test … FAILED` まで「エラー」に数え、
  作業中のエージェントを「停滞・異常」にしてしまう（実際に起きた）。
  単語境界で照合し、パスらしいトークンは除外し、さらに**生の出力が進んでいないこと**を
  裏取りしてから異常と判定する。進捗判定は数字とスピナーを潰した正規化行で行う
  （スピナーの再描画を進捗と見なさないため）。設計原則 4 の具体形。
- 全画面切り替えのレースは「前フレーム比の矩形安定」で判定する（サブディスプレイでのズレ対策）。
- Windows テスト: `powershell -File` 化・cmd ビルトインのスリーパー・パス区切り/コードページ・URL のドライブコロン誤検知に注意。
- macOS 26 で `❯` グリフが消えるフォント問題あり。
- クラッシュは `panic.log` とフレームガードで捕捉する仕組みがある。
- PTY テストのシェルスクリプトに長い `sleep` を書かない（プロセス残留の温床）。

- **git は UI スレッドで待たない。1 回の呼び出しが数秒返ってこない。**
  `git status` は単独なら **0.03 秒**で終わるが、このアプリは同じリポジトリへ
  status スキャン (`git.rs`)・衝突スキャン (`worktree.rs`)・ガターの diff を
  **同時に**撃つ。git はどれも index を触るので取り合いになり、直列化して
  **2.3〜10.2 秒**かかるようになる。そこへ UI スレッドが同期で並ぶと、
  そのままフレームが止まる (実測: `git branch --show-current` が
  **6023ms / thread="main"**、最悪フレーム **4376ms**)。
  - 手本は `Git::branch` / `Git::line_marks` — 裏のスレッド + チャネルで受け、
    UI へは**いま手元にある値**を返す (古くてもよい)。番人テスト
    `git::tests::同期gitをUIスレッドから撃つ経路が残っていない` がある
  - 表示が 1 テンポ遅れるのは許容できるが、**数秒固まるのは許容できない**
  - スキャン間隔は `git::scan_interval` で適応的に空ける (直近の所要時間の 4 倍)。
    固定 TTL のままだと、遅いリポジトリでは前のスキャンが終わった直後に
    次が始まり、**git が常時走っている**状態になる
- **GUI の性能は「印象」ではなく `ZAIVERN_PERF=1` の数値で見る。**
  `perf::dump()` が p50 / p95 / **max** / mean と**再描画の出所**を出す。
  平均だけ見ると数秒の停止を見逃す (p50 10ms・p95 12ms なのに max 4376ms、
  という形で出る)。**max と mean の乖離が停止のサイン**。
  - 背面ウィンドウは macOS が描画を止めるので、計測中は `ctx.request_repaint()`
    で強制的に回すこと。止まったまま計ると「軽い」という嘘の数字が出る
  - 外形の CPU% (`ps`) は実エージェントのプロセスが混ざって交絡する。
    フレーム時間で比べること

- **「遅い」を追うときは、最初に全件を最後まで走らせて分布を取る。**
  仮説から入ると、仮説に合う対策を打って「効かなかった」ことだけが分かる。
  実際に 3 回続けて誤診した (マシン飽和 → 自分が入れた掃除処理 → いずれも外れ)。
  正解は「テストバイナリが 111MB あり、nextest は**テスト 1 件につき 1 プロセス**を
  起こすので 2926 回の起動が競合する。`target` 25GB + ワークツリー群 23GB で
  ページキャッシュが効かない」だった。`--no-fail-fast` で最後まで走らせたら
  **全件通っていた** (CI プロファイルの `terminate-after` が 1 件を落として
  実行を中断していただけ)。
- **計測コマンドが本当に走ったかを、結果を読む前に確かめる。**
  `cargo nextest run --slow-timeout 300s` は**存在しない CLI 引数**で、
  2 回の「計測」が引数エラーで即死していたことに気付かないまま考察を続けた。
  出力が空・短すぎるときは、まず終了コードとエラー行を見る。
- **誤診に基づいて入れた対策は、原因が判明した時点で撤回する。**
  根拠の無い設定を CI に残すと、後から読む人が「何かの理由があるはず」と
  解釈して外せなくなる。実際に nextest の `git` テストグループを撤回した
  (同時実行数を絞るので CI をむしろ遅くする副作用があった)。

## アーキテクチャのメモ

- スマホリモート機能は `remote.rs`（ポート 8899〜）。
- supervisor の診断（Diagnostician）は in-process で完結させる。外部 CLI エージェントへ投げない。
- セッション永続化は `~/.zaivern/sessions/`、PTY 生ログは `~/.zaivern/term_logs/<workspace_hash>/`（フォルダ別）。

## 設計原則 — 競合実装（stablyai/orca）から学んだ5原則

同種の AI エージェント統合エディタである orca（Electron 製・MIT・大規模採用）の実装を分析して得た、
**この製品が守るべき恒久原則**。機能を真似るのではなく、これらの原則で上回ることを目指す。

1. **ターミナルのモデルはウィンドウより長生きさせる。**
   ユーザーが失って困る状態（スクロールバック・PTY・エージェントセッション）は、UI の破棄を生き延びる
   場所に置く。UI はその純粋なビューであって、所有者ではない。

2. **隠れている処理は「欠落あり」で良いが、決してブロックさせない。**
   画面外の消費者には上限付きの末尾だけを渡し、捨てた箇所には明示的なギャップ標識を入れる。
   誰も見ていないビューのために生産者（PTY 読み取り）を止めてはいけない。

3. **アイドル時のコストはゼロでなければならない。**
   再描画は damage 駆動のみ。常時アニメーションはバッテリーのバグである。
   アイドル時の CPU/GPU 使用率は「印象」ではなく数値でリリースゲートにする。
   （競合の最大の不満点がこれ: アイドルで CPU 1コアの約40%を消費している。）

4. **エージェントの状態を画面（ピクセル）から推測しない。**
   優先順位を意識的に降りる: 構造化プロトコル > ベンダー提供フック > 状態ファイル > 画面スクレイプ。
   そして「今どの段にいるか」を UI に出す。最下段はベンダーの CLI 更新のたびに壊れる。

5. **ハンドラは1面、トランスポートは多数。**
   ファイル・git・PTY 操作は抽象ホストに対して一度だけ書き、ローカル / SSH / リモートは
   その実装差ではなくトランスポートの差として扱う。セッションの所有権はアトミックに主張し、
   競合したら fail-closed にする。

### 競合に対する立ち位置（実装判断の指針）

- **ネイティブである利点を捨てない**: 単一バイナリ配布・起動速度・N個の PTY を抱えたときのメモリと
  レイテンシ。競合は1セッションで RSS 1GB 超・インストーラ190MB・並列3セッションで破綻している。
  ここは正面から勝てる領域なので、パフォーマンスは常に計測して回帰させない。
- **CJK/IME を最優先の品質領域とする**: 競合の最大反応数の未修正バグがハングル分解・日本語入力。
  wcwidth 準拠のグリッドと IME 経路は「バグ修正」ではなく差別化機能として扱う。
- **並列エージェントは「衝突を後で発見させない」**: 複数エージェントを走らせる価値は、レビュー時の
  衝突解決コストで相殺される。ディスパッチ前にファイル重複を検出して分割・警告する。
- **承認（approval）はネイティブ UI を持つ**: バイパスフラグを注入するのではなく、全エージェント
  横断の承認キューと永続ポリシー、監査ログを持つ。
