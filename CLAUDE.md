# CLAUDE.md — Zaivern Code 開発ガイド（AIエージェント向け）

このファイルはリポジトリで作業する全ての AI エージェント／開発者が最初に読むこと。

## 絶対ルール

- **ハードコーディング禁止。** どの環境（macOS / Windows / Linux、任意のユーザー名・ホーム・ロケール）でも動くコードのみ。
  - パスは `std::env::temp_dir()` / `dirs` クレート / 設定から導出。`/tmp`・`/Users/...`・`C:\` の直書き禁止。
  - OS 差分は `cfg!(windows)` 等で分岐し、**両側を実装**する。
  - エージェント固有値（resume フラグ、コマンド名等）は `agents.rs` のカタログにデータとして持つ。
- **egui 0.29 固定**（アップグレード禁止）・rustc 1.88+。
- **vendor/vt100 はパッチ済みベンダリング。** `visible_rows` 修正を外すと deep scrollback で debug パニックが再発する。バージョンアップ時は必ずパッチを移植。
- **リリースは checksums.txt を必ず作り、インストーラは必ずそれを検証する。**
  `release.yml` の checksums ジョブ (5 ターゲット + install.sh + install.ps1 + SBOM = **8 行**、
  形式検査つき fail-closed) と、`install.sh` の `verify_checksum` /
  `install.ps1` の `Test-Checksum` は**対**である。片方だけ外してはいけない。
  - **検証は展開の前**（`tar xzf` / `Expand-Archive` より前）。書き出してから
    気付いても、そのファイルはもうディスクにある
  - 取得失敗・行が無い・道具が無い・不一致 — **どの理由でも中止する**。
    「確かめられなかったので、とりあえず入れた」は検証していないのと同じ
  - 番人は `cli::tests::インストーラは展開前にsha256を検証する` と
    `cli::tests::検証できないときは中止する`。**落ちたら直すのは実装であってテストではない**
  - 0.16.0 まで checksums.txt は生成されていたのに、**最重要の自動インストール経路
    (curl | sh / irm | iex) が一度も見ていなかった**。作っているのに使っていない
    チェックサムは、作っていないのと同じ意味しか持たない
- main へ直接コミットしない。作業はブランチ＋隔離ワークツリー（`.claude/worktrees/`）で行う（複数の AI エージェントが同時編集する前提）。
- **統合したワークツリーは即座に消す。** ブランチが統合先の祖先になった時点で
  `git worktree remove` + `git branch -D` まで行う。各ワークツリーは独立した
  `target/` を持つため、放置すると**1つあたり数 GB**積み上がる
  (実測: 15 個で 47GB)。ディスクだけの問題ではなく、ページキャッシュが
  押し出されてテスト実行が数倍遅くなる。
- **`git stash` を使わない。** stash スタックは全ワークツリーで共有されるため、
  同時に動いている別のインスタンスの退避を巻き込む。退避が要るなら WIP コミットを切る。
- **UI から到達できない実装は「未完成」。** 完了報告の前に `cargo check` の
  `never used` 警告を必ず確認し、自分が追加した公開関数・型・コマンドが
  実際に呼ばれていることを確かめる。この警告は「作ったのに繋いでいない」の検出器である。
  到達経路（パレット / メニュー / ボタン / キーバインドのいずれか）を必ず1つ以上用意し、
  用意できないなら README には機能として書かず、未完項目として残す。
- **機能を増やす前に減らせないかを考える。** 同じ操作への到達経路が3つあるなら
  2つ削る。常に0を表示するバッジ、中身より空状態を見せる時間が長いパネル、
  ツールバーとヘッダで重複するボタンは、実装した本人が消す。
- **新しい機能は `src/features/<名前>.rs` を新規作成するだけで足す。
  `app.rs` / `palette.rs` / `feature.rs` / `main.rs` を編集しない。**

  従来は機能を1つ足すたびに **5箇所**（`enum Cmd` / `group_of` /
  `palette_builtin_cmds()` / 巨大な `match cmd` / 描画ループ）を触る必要があり、
  隔離ワークツリーで8本のブランチを同時に走らせたら**全員が同じ5箇所を編集して**
  直列マージが「5箇所 × 8本」の衝突になった。**git worktree はファイルシステムを
  分離するだけで、意味的な衝突は1つも防がない。**

  レジストリ（`src/feature.rs`）で 5箇所 → 1行 まで減らしたが、
  **追記が1行でも残る限り衝突は消えない**（実際に which-key と local_history が
  `config.rs` の設定一覧へ追記して3ハンク衝突した）。**git が衝突を作るのは
  「2つのブランチが同じファイルの近い行を触った」時だけ**なので、共有ファイルを
  1バイトも触らせなければ衝突は構造的に起こり得ない。

  - `build.rs` が `src/features/*.rs` を走査して `mod` 宣言と一覧を `OUT_DIR` へ
    生成する。**生成物はコミットしないので、生成物自体も衝突しない**
  - 機能の実体は `src/<名前>.rs` に置いたままでよい（`src/features/` 側は登録だけ）
  - `Cmd` に variant を増やさず `Cmd::Feature("<module>.<action>")` を通る
  - ID のモジュール接頭辞は `feature::tests::registry_ids_are_unique_and_prefixed`
    が強制する（別ブランチが偶然同じ ID を選ぶと、ディスパッチが先勝ちして
    片方が永久に死ぬため）
  - **`ZaivernApp` のフィールドを公開しない。** 機能側からは `pub(crate)` の
    **メソッド**越しにだけ触る（例: `open_spec_panel`）。これがレジストリの
    適用範囲でもあり、`chord` / `keys` のような内部状態を直接必要とする機能
    （which-key 等）は `app.rs` にグルーが要る。**空の登録を置くのは
    「繋がっている」という嘘になるので禁止**
  - **ゼロにできていない共有面が2つ残る。正直に区別すること**:
    `config.rs`（設定を持つ機能）と `keybinds.rs`（`BindAction` は固定長配列
    `[BindAction; 84]` ＋カウント検査テスト）。どちらも「独立した追記を両方
    残すだけ」の自明な解決で済むが、ゼロではない。打鍵が要る機能は
    「欲しい打鍵」を報告に書き、**統合担当が直列に入れる**

## テストと検証

- **検証は `tools/verify.sh` 一発で終わらせる。** 整形 → コンパイル + 警告 →
  触ったモジュールのテスト、を **1 回のコンパイル**で回す。
  実測 (warm, 1 ファイル変更): 従来の手順 **48 秒 → 26 秒 (46% 短縮)**。
  - `tools/verify.sh` / `tools/verify.sh git:: lsp::` / `--all` / `--quick`
  - 引数無しなら `git status` の `src/*.rs` から対象モジュールを自動で決める
  - **push する前は必ず `tools/verify.sh --lint`**。CI の lint は clippy で、
    rustc の警告より広い。**ローカルの `cargo test` が全部緑でも clippy だけ
    赤くなる** (実際に doc コメントのインデント 1 つ = `doc_overindented_list_items`
    で CI が落ちた)。既定で走らせないのは clippy-driver が別の fingerprint を
    持ち、もう一度フルコンパイルが走るため。債務リストはワークフローから
    毎回読むので、写経してずれることは無い
- **`cargo check --bin zai` だけでは警告を取りこぼす。**
  `#[cfg(test)]` をコンパイルしないので、**テストコードの警告が出ない**。
  実際に `non_snake_case` を 2 回続けて見落とした (しかも 1 回目の「修正」は
  識別子の途中に大文字が残っていて直っていなかった)。
  テストまで見るなら `--all-targets` を付けるか、`cargo test --no-run` を使う。
- **`cargo check` と `cargo test` を両方走らせない (二重払い)。**
  check は `.rmeta` しか作らず test はコード生成まで要るので、**成果物を共有しない**。
  実測 (warm): `check --all-targets` 13 秒 + `test` 18 秒 = 31 秒に対し、
  `cargo test --bin zai --no-run` だけなら 18 秒で**警告も全部出る**。
- **測っていない「効きそうな設定」を残さない。**
  `[profile.dev] debug = "line-tables-only"` を入れてみたが、ターゲットを分けた
  A/B でテストバイナリは **128.7MB のまま 1 バイトも変わらなかった**。
  macOS の dev は `split-debuginfo = "unpacked"` が既定で、デバッグ情報は
  そもそもバイナリに入っていない。**効果ゼロと分かった時点で撤回した。**
- **絶対時間で性能テストの線を引かない。必ず嘘をつく。** 実測で 3 件落ちた:
  `guard` の判定 (Docker の仮想 FS で 1.22 秒)、`highlight` の初回組み立て
  (全 4273 件と同時実行で 1.79 秒)、`lease` の 64 スレッド (負荷で busy)。
  **守りたい性質そのものを測る**: システムコールの呼び出し回数 / 構文セットの
  大きさ / 件数を 2 倍にしたときの伸び (線形性)。比較にしても 2 点の測定が
  別の瞬間なので、負荷の谷と山を引くと跳ねる (4 回に 1 回落ちた)。
  実物: `tools/region-cost.sh` で**同じ点の時間が ×1.31〜2.78 に散る**一方、
  **判定回数はちょうど ×2.00** で線形に出る。
- **ハーネスの費用を測って差し引く。差し引かないと最大 4 割が嘘になる。**
  `tools/region-cost.sh` の実測で、ハーネス率は予約一覧 N=800 で 5.4〜9.1%、
  `text.resolve` 16000 行で 16.6〜24%、`text.anchor` 16000 行で **最大 42.6%**。
  「参照ゲートが遅い」と読めていたものの正体はハーネス (awk のプロセス起動 +
  `mkdir` のスピンロック) で、**実装の遅さではなかった**。
  空回しの区間を必ず 1 本用意して、そのぶんを引いた数字を出すこと。
- **「費用を測る」ハーネスを書くと、頼んでいない二次が出てくる。**
  `region::conflicting_pairs` は総当たりで **O(N²)** だった —
  互いに素な 800 件で **319,600 回 / 9,467ms**。開始行で整列してから掃引する
  形に直して **0 回 / 3.67ms (2581 倍)**。`is_disjoint` は最初の 1 組で降りる
  ので最悪ケースも **319,600 回 → 1 回**。誰も「遅い」と言っていなかった。
- **テスト用のカウンタはスレッドローカルにする。** プロセス共通の
  `static AtomicUsize` にすると、**同時に走っている他のテストの呼び出しまで
  混ざる** (400 回のはずが 800 回になって落ちた)。
- **`allow(dead_code)` を置くと「作ったのに繋いでいない」が見えなくなる。**
  外した瞬間に、錨機構 (`region::resolve` / `capture_anchor` / `follow`) が
  **テストからしか呼ばれていない**ことが露呈した (その後 `lease` / `czero` /
  `coedit` / `zai lease claim` へ繋いだ)。統合の最後に必ず全部外し、
  残った警告は「未完成」として繋ぐか消すかを決めること。
- **テストが拾う「隣の `zai` バイナリ」は版と *mtime* を照合する。**
  `cargo test --bin zai` は bin を作らないので、`target/<profile>/zai` は
  前の実行の残骸かもしれない。古い版は新しいサブコマンドを知らず、
  **`zai` は知らない語をワークスペース指定として扱う**ので GUI を起こして
  落ちる (Docker の Linux で実際に起きた:
  `winit ... neither WAYLAND_DISPLAY nor ... is set`)。
  **`--version` の照合だけでは捕まえられない** — 版を上げずにコードを直した
  日は、**版が同じまま中身だけが古いバイナリ**が隣に居座る。これがいちばん
  質が悪く、「実装したのに直っていない」という嘘の測定結果を出す。
  `test_util::zai_gate_at` は `--version` に加えて**バイナリの mtime を
  `src/` の最新更新と比べ**、古ければ `ZaiVerdict::Stale` として
  `[skip]` と直し方 (`cargo build --bin zai`) を出して降りる。
  判定そのもの (`judge_zai`) は I/O をしない純関数なので表で固定できる。
- **「間欠的」に見えたら、まず乱数種を疑う。** `anyrepo-prove.sh` の二重書き込みは
  「8 回中 3 回」で出ていたが、`--seed 20260818` に固定したら**毎回出た**。
  間欠なのは現象ではなく**入力**だったので、確率的な競合を探して時間を溶かす前に
  **種を固定して決定的にできないか**を試すこと。ハーネスには必ず `--seed` を持たせ、
  失敗した実行の種をログへ出す。
- **錨は、持ち主自身の編集で壊れる。** 行域は行番号ではなく*そこにある内容*
  (先頭行・末尾行) に紐づくので、**自分がその行を書き換えると自分の錨が合わなくなる**。
  すると取り直し (`region::resolve`) が**似た行** (空行 / ` ``` ` / `---`) へ誤マッチし、
  域が別の場所へ「移動」する。移動した先が空いたことになって他人が正当に確保し、
  **2 人が同じ行を書く**。台帳の最終形に重なりは残らないので**台帳を見ても気付けない**。
  → **取り直した結果が記録と違ったら、その読みを採らない** (`src/lease.rs:1394`)。
  `hull` で両方持つ案は、**伸びた域が他人の域を飲み込んで**不変条件を壊したので撤回した。
  試して駄目だった案は消さずに残すこと (同じ案が再提案されるため)。
- **単体テストが全部緑でも、実バイナリを回さないと分からない回帰がある。**
  この版だけで 3 件出た: (1) 64 体で 18/64 しか通らない (CLI が retry 版の
  ロックを使っていなかった) (2) EOF を超える予約が縮んで重なる 2 つの担当が
  同時に載る (3) `--shift` が CLI へ届いていない。**ベンチが毎回「能力検査」を
  してから測る作り**だったから見つかった。検査を省くと「全員書けて衝突 0」と
  いう静かな嘘が出る。
- **`| head` / `| tail` を挟むと `$?` はそちらのものになる。** 終了コードを
  見るときはパイプを外すか `${PIPESTATUS[0]}` を使う (実際に「rc=0 だから
  成功」と読み違えた — 2 度目は「docker が起動していないのに緑」と読んだ)。
  そこで `verify.sh` / `linux-test.sh` / `windows-check.sh` は
  **どの経路で終わっても最後の 1 行に判定を書く** (`✓ … 緑` / `✗ … 赤 (rc=N)`、
  検証していないときは理由つき)。パイプ越しに読んでも嘘にならない。
  番人は `cli::tests::検証スクリプトは終了時に判定行を出す`
  (`exec` で置き換えると trap が発火しないので、それも併せて禁じている)。
- 検証は**ローカル優先**。CI は最小構成 + `Swatinem/rust-cache` 必須。
- **Linux 側は毎回 `tools/linux-test.sh` で確認する。** macOS だけで開発していると
  **Linux / Windows でしか出ない不具合が素通りする**。実際に 43,000 行の変更が
  macOS では全部緑なのに CI の Linux / Windows で落ちた
  (`keybinds::canonical_mods` が非 macOS で ⌃ を ⌘ へ畳むため、
   `⌃⌘F` と `⌘F` が同じ打鍵になり片方が永久に効かなくなっていた)。
  CI 往復は 1 周 5〜6 分かかるうえ切り分けに要る情報が出てこないが、
  Docker なら **1 周 30 秒**で回せる。
  - `tools/linux-test.sh` / `tools/linux-test.sh keybinds::` / `tools/linux-test.sh --check`
  - **ホストの `target/` を汚さないこと** (同じディレクトリを使うと Linux の成果物で
    macOS のビルドが無効化され、戻るたびにフルビルドが走る)。スクリプトは
    `CARGO_TARGET_DIR` を分けている
  - **Docker でだけ落ちるものは追わない**: `app::glyph_tests::*` (フォント無し) /
    `cli::tests::instance_current_uses_own_pid` (PID 名前空間) /
    `terminal::reap_pty_tests::*` (コンテナ内 PTY)。CI の ubuntu では通る
- **Windows 側は `tools/windows-check.sh` で確認する。** `#[cfg(windows)]` のコードは
  macOS のビルドで**一度もコンパイルされない** (src/ の 23 ファイルに 87 箇所ある)。
  手元の `cargo check` が全部緑でも、その中身は未検証のまま残る。
  - `tools/windows-check.sh` — `cargo xwin check --all-targets` (既定・最速)
  - `tools/windows-check.sh --build` — 実際に PE32+ の `zai.exe` を作る (リンクまで通る)
  - `tools/windows-check.sh --clippy` — CI と同じ債務リストで clippy。
    **CI の lint は ubuntu 1 台でしか回らないので、Windows 限定の警告はここでしか出ない**
    (実際に `file_tree.rs` の unused import 2 件がこれで見つかった)
  - `tools/windows-check.sh --gnu` — mingw-w64 経由の `x86_64-pc-windows-gnu`。
    xwin を入れたくない環境向け。src/ に `target_env` の分岐は 1 つも無いので
    cfg 的には msvc と等価
  - 初回だけ `cargo install cargo-xwin --locked` が要る (無ければ手順を出して終了する)。
    素の `cargo check --target x86_64-pc-windows-msvc` は syntect が引く C の
    `onig_sys` が `stdlib.h` を見つけられずに落ちるため**使えない**
  - **担保できないもの**: 実行時の挙動と GUI、それに build.rs の VERSIONINFO 埋め込み。
    Cargo は `[target.'cfg(windows)'.build-dependencies]` を**ホスト**で評価するので、
    macOS からのクロスビルドでは winresource が入らない。ここは CI の windows-latest だけが番人
- **OS で分岐する既定値は、テストも OS 条件を明示する。** `#[cfg(target_os = "macos")]` か
  `cfg!(...)` を期待値に入れる。macOS の予約表と突き合わせるテストを非 mac で走らせると、
  「その OS では使わない打鍵」を咎めるだけになる。
- `terminal::` は実 PTY テスト。1 つの `cargo test` プロセス内で走らせると子プロセスツリーが蓄積し **Linux ランナーを殺す**ため、CI は cargo-nextest（テスト毎プロセス + `pty` test-group で直列化 + slow-timeout のプロセスグループ kill）で実行する。設定は `.config/nextest.toml`。
- ローカル実行例: `cargo nextest run --profile ci`（全量）/ `cargo test session::`（モジュール別）。
- テストは `crate::test_util::unique_temp_dir` を使い、実 `~/.zaivern` に触れない。
- GUI の動作検証はプロセス生存確認で行う（ヘッドレスでは UI を目視できないため）。
- **終了済みセッションへ kill を撃たない**（PID 再利用の巻き添え防止。既存ガードを regress させない）。
- **手元に無い設定が CI にはある。`git config` はスコープを混ぜる。**
  `git config --get-regexp '^filter\.lfs\.'` は system / global / local を**全部混ぜて**返す。
  git-lfs を 1 度でも入れたマシンでは global に置かれるので、**LFS を 1 バイトも
  使っていないリポジトリまで「LFS が居る」**になる。GitHub の ubuntu / macOS /
  windows ランナーは 3 つとも git-lfs 同梱、手元の macOS には 0 件 —
  **CI 3 プラットフォーム全部が赤で手元だけ緑**という形で出た。
  `--show-scope` を付けて `local` / `worktree` だけを「このリポジトリのもの」と数える。
  `core.hooksPath` / `core.sparseCheckout` は global に置いても**このリポジトリの
  挙動を変える**ので全スコープのまま見る — **キーごとに「global でも効くか」が違う。**
  判定は純関数 (`czero_init::parse_scoped_config`) に切り出してテーブルで固定した
  (環境変数を差し替えるテストは並列に走る他のテストへ漏れる)。
- **主張は必ず別のエージェントに反証させる。実装した本人の測定は通さない。**
  この版で 3 つの主張が**全部**崩れた: 「どのリポジトリでも競合ゼロ」(126 回中 42 回で
  衝突が残った) / 「アイドル CPU 0.32〜1.05%」(実測 2.55〜10.00%) /
  「`--shift` の取りこぼしは稀」(6 回中 6 回で 9〜13 体が締め出されていた:
  `51/13 · 54/10 · 53/11 · 55/9 · 54/10 · 54/10`)。
  どれも実装したエージェント自身は緑だと報告していた。
  **反証役には「反証できたら成功」と伝える** — 確認役にすると確認しか返ってこない。
  `--shift` の原因は競合状態ではなく**ずらし上限の固定値**で、0.15.0 の
  `lease::shift_ceiling` で解消した (直した後は 6 回とも 64/0。ただし macOS のみ)。
  経緯は `docs/conflict-zero.md` §6-1。
- **判定関数が、主張している保証そのものを見ているか確かめる。**
  `anyrepo-prove.sh` の `verdict_of()` は `conflict_files` だけを見て `dup_lines` を
  見ていなかった。**中核の主張は「同じ行を 2 人に配らない」** なのに、2 体が同じ行を
  書いても git がたまたま衝突しなければ `proved` と出た。
- **ハーネスは自分で壊した結果を測ることがある。** `anyrepo-prove.sh` は Python の
  `open()` を `newline=""` 無しで使っていたので、**CRLF のファイルを 1 行残らず
  LF へ書き換えて**いた。当然どの段でも衝突し、**実際には守られている
  Windows 由来のリポジトリが「証明できず」**になっていた。
  読み書きで**改行を保存する**こと。「対象を汚していない」の検査は内容まで見る。

## UI の原則

- **画面が突然変わらない。** 何かを起動しただけでレイアウトが激変してはいけない。
  新しい対象はハイライトとスクロール追従で示し、大きな領域を開くのは明示的な操作（Enter 等）を待つ。
- **空白は作らない。** 中身が空のセクションは高さを確保せず、見出しごと消すか畳む。
  空状態は利用可能領域の**中央**に1枚のカードで示す（下や上に取り残さない）。
- **どの幅でも見切れない。** 行は `ui.available_width()` に必ず収める。
  ボタン列は折り返すか、狭いときはアイコンのみへ縮退させる。長い文字列は省略してホバーで全文。
- **中央ビューは単一の列挙型で持つ。** 独立した bool を複数持つと、2つのビューが同時に
  描かれる事故が構造的に起こり得る（実際に起きた）。
- **レイアウト判断は純粋関数に切り出してテーブルテストで固定する。**
  可用幅・件数・最長ラベル幅 → 列幅／折り返し／空状態の有無、そして
  **全ての矩形が可用領域内に収まり重ならないこと**を、極端なサイズ（900×700、1200×300 等）で検証する。

## 既知の罠

- egui-winit 0.29 はペーストコード（⌘V / Ctrl+V）の **press イベントを飲み込む** — クリップボード処理は release イベント側で検知する。
- **egui 0.29 は Shift+ホイールを水平へ寄せる。** `egui-0.29.1/src/input_state/mod.rs` が
  `if modifiers.shift { delta = vec2(delta.x + delta.y, 0.0) }` としているので、
  Shift を押している間 `raw_scroll_delta.y` は**常に 0**。⌘V の飲み込みと同じ型の罠で、
  素直に `.y` を読むと「Shift+ホイールが何も起こさない」になる。
  読み直しは `terminal::wheel_dy` に 1 本だけ置く。
- **代替画面にも履歴はある** (vendor/vt100 のパッチ)。スクロール領域を使わずに
  画面を流すアプリ (ストリームするエージェント) の行は代替グリッドの履歴へ積まれる。
  ここを「代替画面に履歴は無い」と決め打つと 2 つ壊れる:
  (1) `scrolled_delta` が 0 を返すと `count_around` が vt100 の増やした戻り量を
  **巻き戻す**ので、遡って見ている位置が出力のたびに下へ引き戻される
  (2) 押し出しを誰も数えないので選択の絶対行が更新されず、**コピーだけが静かにずれる**。
  数えないのは**グリッドをまたいだ 2 点だけ** (`before.alt != after.alt`)。
  通し番号の台帳は `LineIndex` がグリッドごとに別に持つ — シェル統合が読むのは
  通常画面側だけで、混ぜると vim を 1 回通しただけでマーカー行がずれる。
- **全画面のまま同じ場所へ描き直すアプリには、端末側に履歴が 1 行も無い。**
  Claude Code v2 の実ログ 1.39MB を再生すると **LF 0 回 / SU 0 回 / CUP 3 万回**
  (代替画面へ入って出ない)。「遡って選べない」の原因が実装とは限らないので、
  直す前に**生ログを再生して押し出し行数を数える**こと。
  - **端末側を賢くしても解けない。無いものは遡れない。** 解けるのは
    **アプリに全画面をやめさせる**ときだけで、それは端末の仕事ではなく
    **起動時の env** の仕事になる。`agents::TERMINAL_SCROLLBACK_ENV` が
    その置き場 (claude → `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1`)。
    実測 (claude 2.1.233 / 120x40 / 起動 8 秒): 既定は `?1049h` 1 回・**改行 0 回**、
    env ありは `?1049h` **0 回**・改行 14 回・マウス捕捉も無し
  - **env は「入れた」だけでは効かない。対照を取らないと嘘の緑になる。**
    `CLAUDE_CONFIG_DIR` を一時ディレクトリへ向けた検査は、**素の claude まで
    全画面へ入らなくなる** (未認証のオンボーディング画面) ので、env を `0` に
    戻しても緑のままだった。**対照 (env 無し) が悪い方を再現しない環境では
    `[skip]` する**のが正しい (`terminal::tests::claude_codeは履歴用envで代替画面をやめる`)
  - **フラグや env を捏造しない。** 実在するかは
    `strings <CLI 本体> | grep -oE 'CLAUDE_CODE_[A-Z0-9_]+'` のように**実物から**確かめ、
    さらに PTY で起動して**出力バイト列が変わることまで**見る
- **通常画面でアプリがマウス報告していても、ホイールは端末の履歴が優先。**
  gemini は `?1006h` を出す (クリックのため) が画面は流れていて、履歴は
  こちら側に積まれている (実測 20 秒で改行 133 回)。ここでアプリへ渡すと
  「積まれているのに遡れない」になる。渡すのは**代替画面のときだけ**
  (`terminal::wheel_route`)。実測: codex / gemini / cursor-agent / droid は
  4 つとも代替画面を使わない — **代替画面を使うのは Claude Code だけだった**
- **飲み込まれるのは V だけではない。** `egui-winit-0.29.1/src/lib.rs:758-774` は
  押下イベントが `modifiers.command && key ∈ {X, C, V}` に当たると
  `Event::Cut / Copy / Paste` を積んで **その場で return** する。判定は
  **shift / alt の有無を見ていない**ので、`⌘⇧C`（Cockpit）や `⌘⇧V`（プレビュー）は
  `consume_shortcut` では**構造的に絶対発火しない**（実際に「ツールチップに ⌘⇧C と
  出ているのに効かない」として報告された）。`command` は mac では ⌘、
  Windows/Linux では **Ctrl** に写るので、`Ctrl+Shift+C` も同じく死ぬ。
  ショートカットの消費は必ず `keybinds::consume_shortcut_compat` を通すこと
  （すり替えを逆再生して拾い直す。素の ⌘C/⌘V は横取りしない）。
- **画面に出す打鍵表記をベタ書きしない。** `tr("… (⌘⇧C)")` は
  (1) config.toml で再割り当てされた瞬間に嘘になり (2) Windows/Linux では表記そのものが違う。
  必ず `keybinds::format_shortcut(keys.get(BindAction::X))`（app 内は `self.key_hint`,
  `Keybinds` を持てない描画側は `keybinds::key_hint(ctx, ..)`）から生成する。
  `keybinds::tests::画面のショートカット表記をベタ書きしていない` が番人。
- **キーバインドは macOS の予約表と突き合わせる。** `keybinds::MACOS_RESERVED` は
  `~/Library/Preferences/com.apple.symbolichotkeys.plist` の enabled 項目から起こした実測表。
  `⌘⌥D`（Dock）だけでなく **`⌃Space`（前の入力ソース: 日本語 IME を使うと必ず有効）**、
  `⌘\``（次のウィンドウ）、`⌘Space` / `⌥⌘Space` なども取られている。
- **ソースを読む回帰テストは改行を正規化する。** `include_str!` の結果を
  `.replace("\r\n", "\n")` に通してから検索する。Windows のチェックアウトは CRLF なので、
  `"\n        });\n"` のようなパターンは必ず外れる。
- **macOS の `⌘⌥D` は OS（Dock の自動的に隠す）に予約されている。** アプリまで届かない。
  キーバイントを決めるときは OS の予約と既存割り当ての両方を確認する。
- **端末セルは整数ピクセルに揃える。** `col * cell_w` を小数のまま使うと、epaint が
  文字位置だけを丸めるため桁間隔が 8/8/7/8 px と揺れ、文字がガタガタに見える。
  100% 表示（Windows に多い）で最も悪化する。`theme::snap_len` を通すこと。
- **【作業中】`src/highlight.rs` は 400KB (`MAX_HIGHLIGHT_BYTES`) を超えると
  強調表示を丸ごと捨てて素のテキストを返す。** つまり**このリポジトリ自身の
  `src/app.rs` (2.0MB) が白一色で表示される** — いちばん大きいファイル、
  つまりいちばん読むのがつらいファイルだけが色を失う。
  `bracket_pairs` も同じ上限で空を返す。
  **置き換え (`Highlighter::layout_job_visible` / `advance_to_visible` =
  可視域だけ塗る) は `highlight.rs` に入ったが、`app.rs` はまだ
  `layout_job` を呼んでいるので挙動は変わっていない** (2026-08-12 時点)。
  「UI から到達できない実装は未完成」の実例なので、繋ぐまでは直ったと書かない。
- **プロセスを殺すときは必ずツリーごと。** 直接の子だけを kill すると、シェルが
  `exec` せずに起動した孫がパイプを握ったまま残り、読み取りの join が孫の寿命まで戻らない
  （= UI が固まる）。`procx::kill_tree` を使い、unix では子を独立したプロセスグループで起動する。
- **`kill` に負の PID を渡すときは `--` を付ける。** Linux の procps-ng は `-<pid>` を
  短オプションの塊として解釈し、PID が 1 で始まると `kill(-1, SIGKILL)` になって
  ユーザーの全プロセスを巻き込む（実際に起きた）。
- **egui 0.29 の ID はラベル文字列由来ではない。** `Button` / `SelectableLabel` /
  `Checkbox` / `RadioButton` / `Link` / salt 無しの `TextEdit` は `allocate_*` 経由の
  **自動カウンタ**から ID を作るので、同じラベルが並んでも衝突しない
  （同一ラベル5個 → ID は5種類。ヘッドレステストで実証済み）。
  本当に衝突するのは `make_persistent_id` を通る系統だけ:
  `ScrollArea` / `ComboBox` / `CollapsingHeader` / `Grid` / salt 付き `TextEdit`。
  可変長リストの中でこれらを使うときは `ui.push_id` か salt に要素の ID を混ぜる
  （`src/panels.rs` の `mod egui_id_guard` が構造検査で自動検出する）。
  **「同じ見た目のボタンが並ぶと最後しか押せない」は、この egui では起こらない。**
  そう見えたら原因はアプリ側のロジック（index と ID の取り違え等）なので、そちらを疑うこと。
- **egui 0.29 の `RichText::strong()` は太字にならない。色を変えるだけである。**
  しかも `.color()` を先に指定していると `get_text_color` が色のほうを採るので、
  **`strong()` は 1 ピクセルも効かない**。同梱フォントに太字フェイスも無い。
  実際に Markdown の見出しも `**強調**` も本文と**まったく同じ見た目**で
  描かれていた。太く見せたいなら同じ galley を `BOLD_OFFSET` (0.6px) ずらして
  **2 度描く** synthetic bold にする（`markdown::bold_label`）。
- **`egui::Grid::max_col_width` は「全列共通の上限」であって列ごとの幅ではない。**
  さらに Grid のセルの中では `ui.available_width()` が**列幅ではなく
  `max_col_width`** を返す（最終列だけ残り全幅）。だから余白で右揃えを作ると
  「余白 + 本文」がちょうど幅に等しくなり、`horizontal_wrapped` が折り返して
  **2 行目が左端へ落ちる**。しかもその高さは行に数えられないので
  **次の行と重なる**。列幅は自分で決め（`markdown::table_col_widths`）、
  寄せは galley の `halign` で行うこと。
- **画面テキストの部分一致で状態を判定しない。** `error` / `failed` を部分一致で拾うと
  `Read(src/error_handling.rs)` や `test … FAILED` まで「エラー」に数え、
  作業中のエージェントを「停滞・異常」にしてしまう（実際に起きた）。
  単語境界で照合し、パスらしいトークンは除外し、さらに**生の出力が進んでいないこと**を
  裏取りしてから異常と判定する。進捗判定は数字とスピナーを潰した正規化行で行う
  （スピナーの再描画を進捗と見なさないため）。設計原則 4 の具体形。
- 全画面切り替えのレースは「前フレーム比の矩形安定」で判定する（サブディスプレイでのズレ対策）。
- Windows テスト: `powershell -File` 化・cmd ビルトインのスリーパー・パス区切り/コードページ・URL のドライブコロン誤検知に注意。
- macOS 26 で `❯` グリフが消えるフォント問題あり。
- クラッシュは `panic.log` とフレームガードで捕捉する仕組みがある。
- **Windows はファイル削除が *delete pending* を経る。** 最後のハンドルが閉じる
  まで中間状態が残り、そのあいだに `create_new` すると `AlreadyExists` ではなく
  **`ACCESS_DENIED` (os error 5)** が返る。64 体がロックを奪い合うと必ず踏み、
  「壊れている」と扱うと**いちばん混んでいるとき = いちばん衝突しやすいとき
  にだけ**台帳が使えなくなる。Windows でだけ取り合いとして待つこと
  (`lease::lock_contended`)。unix の `PermissionDenied` は本物の権限問題なので
  即失敗のままにする。
- **`cmd /C` にコマンド全体を 1 引数で押し込まない。** Rust の `Command` は
  引数ごとに Windows の規則で引用するので、cmd 側の再解析とずれて失敗する。
  引数は分けて渡す。unix もシェルを挟まず実体 (`mv` 等) を直に起こすほうが安全。
- **`thread_local!` に `///` を付けない。** rustdoc はマクロ展開の中身を
  文書化しないので `unused_doc_comments` で `-D warnings` が落ちる。通常の
  `//` にする。**`tools/verify.sh --lint` を push 前に必ず通すこと** —
  `cargo test` が全部緑でも clippy だけ赤くなる。
- **固定の待ち予算は N が増えれば必ず破綻する。** 臨界区間が t なら N 体が
  順に通るには N·t 要るので、定数は「N がいくつまで大丈夫か」を暗黙に決めて
  しまう (64 体には 320ms 要るのに予算 200ms だった)。**進捗が観測できる限り
  待ちを延ばす**。台帳だけでなく**ロックファイルの更新時刻も見る** —
  `with_store` は中身が変わったときしか書かないので、断られ続ける局面では
  台帳が 1 度も動かず「詰まっている」と区別が付かない。
- **「拒否が多い」を容量の問題と決めつけない。** 実測で 53 件断られていたのは
  空きが無いからではなく**誰もずらしていなかった**から (要求範囲 132 行に対し
  空き 1868 行、必要 573 行)。**供給と需要を先に数える**こと。
- **`SAFE_BAND` は 2 つずつの組で検証したが、衝突は組ごとに決まらない。**
  A が 17 行目、B が 5 / 13 / 25 行目を編集 (**どの組も 4 行以上離れており
  `SAFE_BAND=3` を満たす**) → 内容が周期的 (``` の連続 / 生成コード / 同じ行の反復)
  だと**衝突する**。各行が異なる内容なら綺麗に通る。**zaivern を 1 行も使わない
  素の git で再現する** (`tools/merge-band-probe.sh --mode bracket` が rc=1)。
  周期的な内容では diff がハンクを別の位置へ合わせるので、**行距離では
  ハンク境界が決まらない**。
  **「同じ行を 2 人に配らない」(所有) は成り立っている** — 126 回中 126 回で
  `dup_lines = 0`。崩れたのは「互いに素な行域 ⇒ 綺麗にマージできる」という**推論**。
  この 2 つを文書でも出力でも混ぜないこと。
- **エージェントがまだ動いているうちにワークツリーを消さない。**
  「統合したら即座に消す」は正しいが、**そのブランチのエージェントが終わってから**。
  完了通知が来た後も、エージェントは追加の指示で再開しうる (実際に 34 実行が
  `getcwd` エラーで中断した)。消す前に `ListAgents` で状態を見ること。
- **サブエージェントに実 `~/.zaivern` を触らせない。** この版で 2 件起きた
  (うち 1 件は**他インスタンスの生きたリースかもしれないファイルを実際に削除**)。
  複数インスタンス同時編集が前提のリポジトリでは、台帳の削除は他人の作業を壊す。
  指示に毎回「テストは `ZAIVERN_HOME` を一時ディレクトリへ向ける」と書く
  (`config::zaivern_dir()` が対応済み)。**掃除は「自分が作ったと追跡できるもの」
  だけ**で、パターン検索で見つけたものは消さない。
- **`SAFE_BAND = 3` の由来はパッチ適用 (`git apply`) である。** 三方向マージ
  (`git merge` / `merge-tree`) は **1 行離れていれば足りる** (myers / minimal /
  patience / histogram の 4 アルゴリズムすべてで実測)。「diff の既定文脈が
  3 行だからハンクが畳まれる」は誤りで、3 行は*表示*の話。`git merge` だけを
  保証する層は `region::MERGE_ONLY_BAND` を使ってよい (`coedit` は帯 1 で
  証明できる組が 72 → 122 本、見逃しは 0 のまま)。
- **サブエージェントは統合後にコミットを作り直すことがある。** 追加の指示を
  送ると、こちらが merge 済みの commit を amend した別 SHA が枝先に残る。
  `git merge-base --is-ancestor` で確認し、**取り込むと巻き戻る変更が無いか**を
  関数一覧の差分で見てから判断すること。
- PTY テストのシェルスクリプトに長い `sleep` を書かない（プロセス残留の温床）。

- **git は UI スレッドで待たない。1 回の呼び出しが数秒返ってこない。**
  `git status` は単独なら **0.03 秒**で終わるが、このアプリは同じリポジトリへ
  status スキャン (`git.rs`)・衝突スキャン (`worktree.rs`)・ガターの diff を
  **同時に**撃つ。git はどれも index を触るので取り合いになり、直列化して
  **2.3〜10.2 秒**かかるようになる。そこへ UI スレッドが同期で並ぶと、
  そのままフレームが止まる (実測: `git branch --show-current` が
  **6023ms / thread="main"**、最悪フレーム **4376ms**)。
  - 手本は `Git::branch` / `Git::line_marks` — 裏のスレッド + チャネルで受け、
    UI へは**いま手元にある値**を返す (古くてもよい)。番人テスト
    `git::tests::描画スレッドから同期gitを撃つ経路が残っていない` がある
  - 表示が 1 テンポ遅れるのは許容できるが、**数秒固まるのは許容できない**
  - スキャン間隔は `git::scan_interval` で適応的に空ける (直近の所要時間の 4 倍)。
    固定 TTL のままだと、遅いリポジトリでは前のスキャンが終わった直後に
    次が始まり、**git が常時走っている**状態になる
- **GUI の性能は「印象」ではなく `ZAIVERN_PERF=1` の数値で見る。**
  `perf::dump()` が p50 / p95 / **max** / mean と**再描画の出所**を出す。
  平均だけ見ると数秒の停止を見逃す (p50 10ms・p95 12ms なのに max 4376ms、
  という形で出る)。**max と mean の乖離が停止のサイン**。
  - 背面ウィンドウは macOS が描画を止めるので、計測中は `ctx.request_repaint()`
    で強制的に回すこと。止まったまま計ると「軽い」という嘘の数字が出る
  - 外形の CPU% (`ps`) は実エージェントのプロセスが混ざって交絡する。
    フレーム時間で比べること

- **「遅い」を追うときは、最初に全件を最後まで走らせて分布を取る。**
  仮説から入ると、仮説に合う対策を打って「効かなかった」ことだけが分かる。
  実際に 3 回続けて誤診した (マシン飽和 → 自分が入れた掃除処理 → いずれも外れ)。
  正解は「テストバイナリが 111MB あり、nextest は**テスト 1 件につき 1 プロセス**を
  起こすので 2926 回の起動が競合する。`target` 25GB + ワークツリー群 23GB で
  ページキャッシュが効かない」だった。`--no-fail-fast` で最後まで走らせたら
  **全件通っていた** (CI プロファイルの `terminate-after` が 1 件を落として
  実行を中断していただけ)。
- **計測コマンドが本当に走ったかを、結果を読む前に確かめる。**
  `cargo nextest run --slow-timeout 300s` は**存在しない CLI 引数**で、
  2 回の「計測」が引数エラーで即死していたことに気付かないまま考察を続けた。
  出力が空・短すぎるときは、まず終了コードとエラー行を見る。
- **誤診に基づいて入れた対策は、原因が判明した時点で撤回する。**
  根拠の無い設定を CI に残すと、後から読む人が「何かの理由があるはず」と
  解釈して外せなくなる。実際に nextest の `git` テストグループを撤回した
  (同時実行数を絞るので CI をむしろ遅くする副作用があった)。

## アーキテクチャのメモ

- スマホリモート機能は `remote.rs`（ポート 8899〜）。
- supervisor の診断（Diagnostician）は in-process で完結させる。外部 CLI エージェントへ投げない。
- **`~/.zaivern` の下の置き場は全部「ワークスペースキー」で決まる。**
  新しく書く値は 2 つだけ — 単一フォルダの `history::workspace_key` と、
  ルート集合の `history::workspace_set_key`。`fnv1a64` 由来で**版に依存しない**
  (`DefaultHasher` は std が版をまたぐ安定性を保証していないので、rustc を上げると
  利用者から見て台帳もセッションもログも全部消える)。
  自前で 16 桁キーを作ると `history::tests::ワークスペースキーを計算するのはこのモジュールだけ`
  が落ちる。旧キーは 3 種類あり `legacy_keys_of` + `adopt_keys_in` が改名で引き取る。
  詳細と正規化の規則は `docs/workspace-key.md`。
  セッション永続化は `~/.zaivern/sessions/<集合キー>.toml`、PTY 生ログは
  `~/.zaivern/term_logs/<キー>/`（フォルダ別）。

## 設計原則 — 競合実装（stablyai/orca）から学んだ5原則

同種の AI エージェント統合エディタである orca（Electron 製・MIT・大規模採用）の実装を分析して得た、
**この製品が守るべき恒久原則**。機能を真似るのではなく、これらの原則で上回ることを目指す。

1. **ターミナルのモデルはウィンドウより長生きさせる。**
   ユーザーが失って困る状態（スクロールバック・PTY・エージェントセッション）は、UI の破棄を生き延びる
   場所に置く。UI はその純粋なビューであって、所有者ではない。

2. **隠れている処理は「欠落あり」で良いが、決してブロックさせない。**
   画面外の消費者には上限付きの末尾だけを渡し、捨てた箇所には明示的なギャップ標識を入れる。
   誰も見ていないビューのために生産者（PTY 読み取り）を止めてはいけない。

3. **アイドル時のコストはゼロでなければならない。**
   再描画は damage 駆動のみ。常時アニメーションはバッテリーのバグである。
   アイドル時の CPU/GPU 使用率は「印象」ではなく数値でリリースゲートにする。
   （競合の最大の不満点がこれ: アイドルで CPU 1コアの約40%を消費している。）

4. **エージェントの状態を画面（ピクセル）から推測しない。**
   優先順位を意識的に降りる: 構造化プロトコル > ベンダー提供フック > 状態ファイル > 画面スクレイプ。
   そして「今どの段にいるか」を UI に出す。最下段はベンダーの CLI 更新のたびに壊れる。

5. **ハンドラは1面、トランスポートは多数。**
   ファイル・git・PTY 操作は抽象ホストに対して一度だけ書き、ローカル / SSH / リモートは
   その実装差ではなくトランスポートの差として扱う。セッションの所有権はアトミックに主張し、
   競合したら fail-closed にする。

### 競合に対する立ち位置（実装判断の指針）

- **ネイティブである利点を捨てない**: 単一バイナリ配布・起動速度・N個の PTY を抱えたときのメモリと
  レイテンシ。競合は1セッションで RSS 1GB 超・インストーラ190MB・並列3セッションで破綻している。
  ここは正面から勝てる領域なので、パフォーマンスは常に計測して回帰させない。
- **CJK/IME を最優先の品質領域とする**: 競合の最大反応数の未修正バグがハングル分解・日本語入力。
  wcwidth 準拠のグリッドと IME 経路は「バグ修正」ではなく差別化機能として扱う。
- **並列エージェントは「衝突を後で発見させない」**: 複数エージェントを走らせる価値は、レビュー時の
  衝突解決コストで相殺される。ディスパッチ前にファイル重複を検出して分割・警告する。
- **承認（approval）はネイティブ UI を持つ**: バイパスフラグを注入するのではなく、全エージェント
  横断の承認キューと永続ポリシー、監査ログを持つ。
