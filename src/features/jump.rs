//! ジャンプモード (2 打鍵で画面上の語へ飛ぶ) の**登録だけ**。
//!
//! 実体と `FEATURE` の定義は [`crate::jump`] にあるので、ここは**再エクスポート
//! するだけ**。定義を写経して 2 か所に持つとズレる (`draw: Some(..)` や
//! `settings` のような後から足された要素を取りこぼす)。

pub use crate::jump::FEATURE;
