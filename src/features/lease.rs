//! ファイル所有リース (並列エージェントの衝突を発生させない) の登録。
//!
//! 実体と `FEATURE` の定義は [`crate::lease`] にあるので、ここは**再エクスポート
//! するだけ**。定義を写経して 2 か所に持つとズレる (`draw: Some(...)` のような
//! 後から足された要素を取りこぼす)。

pub use crate::lease::FEATURE;
