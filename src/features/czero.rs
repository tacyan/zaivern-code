//! 🛟 競合ゼロ点検 (いま自分がどこまで守られているか) の**登録だけ**。
//! 実体は `src/czero.rs`。
//!
//! `main.rs` の `mod` 一覧にも `feature.rs` のレジストリにも触らないため、
//! 実体をここから `#[path]` で取り込む。**共有ファイルへの追記が 1 行も無い**
//! ので、別のブランチが同時に機能を足しても構造的に衝突しない。

#[path = "../czero.rs"]
mod imp;

pub use imp::FEATURE;
